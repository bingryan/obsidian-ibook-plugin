# Obsidian ibook Plugin(WIP)

```sql
CREATE TABLE ZAEANNOTATION (
	Z_PK INTEGER PRIMARY KEY,
	Z_ENT INTEGER,
	Z_OPT INTEGER,
	ZANNOTATIONDELETED INTEGER, # 注释已经删除
	ZANNOTATIONISUNDERLINE INTEGER, # 注释是下划线
	ZANNOTATIONSTYLE INTEGER, # 注释样式
	ZANNOTATIONTYPE INTEGER, # 注释类型
	ZPLABSOLUTEPHYSICALLOCATION INTEGER, # 平面物理分配
	ZPLLOCATIONRANGEEND INTEGER, # 定位范围
	ZPLLOCATIONRANGESTART INTEGER, # 位置范围开始
	ZANNOTATIONCREATIONDATE TIMESTAMP, # 注释创建日期
	ZANNOTATIONMODIFICATIONDATE TIMESTAMP, # 注释修改日期
	ZANNOTATIONASSETID VARCHAR, # 注释资产 ID
	ZANNOTATIONCREATORIDENTIFIER VARCHAR,
	ZANNOTATIONLOCATION VARCHAR, # 位置
	ZANNOTATIONNOTE VARCHAR, # 注释
	ZANNOTATIONREPRESENTATIVETEXT VARCHAR, # 注释创建者标识符
	ZANNOTATIONSELECTEDTEXT VARCHAR, # 注释所选文本
	ZANNOTATIONUUID VARCHAR,
	ZFUTUREPROOFING1 VARCHAR,
	ZFUTUREPROOFING10 VARCHAR,
	ZFUTUREPROOFING11 VARCHAR,
	ZFUTUREPROOFING12 VARCHAR,
	ZFUTUREPROOFING2 VARCHAR,
	ZFUTUREPROOFING3 VARCHAR,
	ZFUTUREPROOFING4 VARCHAR,
	ZFUTUREPROOFING5 VARCHAR,
	ZFUTUREPROOFING6 VARCHAR,
	ZFUTUREPROOFING7 VARCHAR,
	ZFUTUREPROOFING8 VARCHAR,
	ZFUTUREPROOFING9 VARCHAR,
	ZPLSTORAGEUUID VARCHAR,
ZPLUSERDATA BLOB 
);

CREATE TABLE ZBKCOLLECTION (
	Z_PK INTEGER PRIMARY KEY,
	Z_ENT INTEGER,
	Z_OPT INTEGER,
	ZDELETEDFLAG INTEGER,
	ZHIDDEN INTEGER,
	ZPLACEHOLDER INTEGER,
	ZSORTKEY INTEGER,
	ZSORTMODE INTEGER,
	ZVIEWMODE INTEGER,
	ZLASTMODIFICATION TIMESTAMP,
	ZLOCALMODDATE TIMESTAMP,
	ZCOLLECTIONID VARCHAR,
	ZDETAILS VARCHAR,
	ZTITLE VARCHAR 
);

CREATE TABLE ZBKLIBRARYASSET (
	Z_PK INTEGER PRIMARY KEY,
	Z_ENT INTEGER,
	Z_OPT INTEGER,
	ZCANREDOWNLOAD INTEGER,
	ZCOMBINEDSTATE INTEGER,
	ZCOMPUTEDRATING INTEGER,
	ZCONTENTTYPE INTEGER, # 内容类型
	ZDESKTOPSUPPORTLEVEL INTEGER,
	ZDIDRUNFORYOUENDOFBOOKEXPERIENCE INTEGER,
	ZDIDWARNABOUTDESKTOPSUPPORT INTEGER,
	ZFILESIZE INTEGER, # 文件大小
	ZGENERATION INTEGER,
	ZHASRACSUPPORT INTEGER,
	ZISDEVELOPMENT INTEGER,
	ZISDOWNLOADINGSUPPLEMENTALCONTENT INTEGER,
	ZISEPHEMERAL INTEGER,
	ZISEXPLICIT INTEGER,
	ZISFINISHED INTEGER,
	ZISHIDDEN INTEGER,
	ZISLOCKED INTEGER,
	ZISNEW INTEGER,
	ZISPROOF INTEGER,
	ZISSAMPLE INTEGER,
	ZISSTOREAUDIOBOOK INTEGER,
	ZISSUPPLEMENTALCONTENT INTEGER,
	ZISTRACKEDASRECENT INTEGER,
	ZMETADATAMIGRATIONVERSION INTEGER,
	ZNOTFINISHED INTEGER,
	ZPAGECOUNT INTEGER, # 页数
	ZRATING INTEGER,
	ZSERIESISCLOUDONLY INTEGER,
	ZSERIESISHIDDEN INTEGER,
	ZSERIESNEXTFLAG INTEGER,
	ZSERIESSORTKEY INTEGER,
	ZSORTKEY INTEGER,
	ZSTATE INTEGER,
	ZTASTE INTEGER,
	ZTASTESYNCEDTOSTORE INTEGER,
	ZLOCALONLYSERIESITEMSPARENT INTEGER,
	ZPURCHASEDANDLOCALPARENT INTEGER,
	ZSERIESCONTAINER INTEGER,
	ZSUPPLEMENTALCONTENTPARENT INTEGER,
	ZASSETDETAILSMODIFICATIONDATE TIMESTAMP,
	ZBOOKHIGHWATERMARKPROGRESS FLOAT, # 阅读进度
	ZBOOKMARKSSERVERMAXMODIFICATIONDATE TIMESTAMP,
	ZCREATIONDATE TIMESTAMP, # 创建时间
	ZDATEFINISHED TIMESTAMP, # 完成时间
	ZDURATION FLOAT, # 持续时间
	ZEXPECTEDDATE TIMESTAMP,
	ZFILEONDISKLASTTOUCHDATE TIMESTAMP,
	ZLASTENGAGEDDATE TIMESTAMP, # 上次关闭时间
	ZLASTOPENDATE TIMESTAMP, # 上次打开时间
	ZLOCATIONSERVERMAXMODIFICATIONDATE TIMESTAMP,
	ZMODIFICATIONDATE TIMESTAMP, # 上次修改时间
	ZPURCHASEDATE TIMESTAMP, # 购买时间
	ZREADINGPROGRESS FLOAT, # 
	ZRELEASEDATE TIMESTAMP,
	ZUPDATEDATE TIMESTAMP,
	ZVERSIONNUMBER FLOAT, # 版本号
	ZACCOUNTID VARCHAR,
	ZASSETGUID VARCHAR,
	ZASSETID VARCHAR, # 资产ID
	ZAUTHOR VARCHAR, #作者
	ZBOOKDESCRIPTION VARCHAR, # 书本描述
	ZBOOKMARKSSERVERVERSION VARCHAR,
	ZCOMMENTS VARCHAR, #评论
	ZCOVERURL VARCHAR, #ZCOVERURL
	ZCOVERWRITINGMODE VARCHAR,
	ZDATASOURCEIDENTIFIER VARCHAR,
	ZDOWNLOADEDDSID VARCHAR,
	ZEPUBID VARCHAR,
	ZFAMILYID VARCHAR,
	ZGENRE VARCHAR, # 题材
	ZGROUPING VARCHAR,
	ZKIND VARCHAR,
	ZLANGUAGE VARCHAR,
	ZLOCATIONSERVERVERSION VARCHAR,
	ZPAGEPROGRESSIONDIRECTION VARCHAR,
	ZPATH VARCHAR, # 路径
	ZPERMLINK VARCHAR,
	ZPURCHASEDDSID VARCHAR,
	ZSEQUENCEDISPLAYNAME VARCHAR,
	ZSERIESID VARCHAR,
	ZSERIESSTACKIDS VARCHAR,
	ZSORTAUTHOR VARCHAR,
	ZSORTTITLE VARCHAR,
	ZSTOREID VARCHAR,
	ZSTOREPLAYLISTID VARCHAR,
	ZTEMPORARYASSETID VARCHAR,
	ZTITLE VARCHAR,
	ZVERSIONNUMBERHUMANREADABLE VARCHAR,
	ZYEAR VARCHAR 
);
```
